#include "Timeliner.h"

json Timeliner(){

    json TimelineUnord = NULL;
    json Timeline = NULL;

    // group events by transaction if; one item per buy 
    // order timeline

    return Timeline;
}