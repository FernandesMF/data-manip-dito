#include "Timeliner.h"

#pragma once

int main(){

    json events = NULL;

    // read events file
    // transform them into a timeline
    // write timeline to external file
    // print timeline to screen?

    return 0;
}
